#include "main.h"

/**
 * mul - is a function that multiplies two integers.
 * @a: the first integer.
 * @b: the second integer.
 * Return: the result of a * b .
 */
int mul(int a, int b)
{
	return (a * b);
}
